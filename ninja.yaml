pkg:  ninja
url:  github.com/ninja-build/ninja
desc: a small build system with a focus on speed

pack:
  - path: build-cmake/ninja
    type: bin

install:
  - cmake -Bbuild-cmake
  - cmake --build build-cmake
