pkg:  tmux
url:  github.com/tmux/tmux
desc: tmux is a terminal multiplexer.

collection: core

install:
  - sh autogen.sh
  - ./configure
  - make
  - make prefix=$HOME/.hayashi/pack install

update:
  - make
  - make prefix=$HOME/.hayashi/pack install
